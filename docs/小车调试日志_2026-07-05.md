# 精灵小车 STM32 调试日志

## 日期
2026-07-05

## 当前状态
- 用户对话窗口即将用完，需要开启新对话继续
- 小车已烧录最新固件（2026-07-05 19:48 编译）
- 左后轮驱动板硬件损坏，无法修复（二手设备，买不到替换件）

---

## 硬件问题

### 故障确认
- **左后轮驱动板损坏**：接收总线指令 `#008P...` 无响应
- **右后轮正常**：接收总线指令 `#009P...` 正常转动
- **两个前轮正常**

### 已尝试的排查
1. 检查驱动板 ID（无拨码开关，固定 ID）
2. 交叉测试信号线（确认是驱动板本身故障）
3. 拔掉左后轮电源线（减少电磁阻力）
4. 轮子能用手拨动，机械结构无卡死

### 最终结论
左后轮驱动板硬件损坏，无法通过软件修复。

---

## 软件修改记录

### 当前代码位置
`C:\Users\26630\Desktop\新建文件夹 (3)\0012-STM32-精灵小车-step1\0012-精灵小车-Step1\02 程序资料\02 出厂程序源码\`

### 已修改的文件
1. **USER/Main.c**
   - 加入了 `Sys_Mode` 遥控/自主模式分离
   - 加入了 `Loop_Vision_Auto()` 视觉自主模式框架
   - 加入了 PS2 手柄 SELECT/START 组合键检测宏
   - `Motors_Run()` 已加入左后轮损坏补偿逻辑（compensate 变量）
   - 加入了非阻塞机械臂抓取状态机（齿轮/螺母）

2. **Hardware/Usart.c**
   - USART2 中断加入 ESP32 视觉单字节指令接收（0x01=齿轮, 0x02=螺母）

### Motors_Run 补偿代码（当前版本）
```c
void Motors_Run(int Left_Front_Speed, int Right_Front_Speed, int Left_Back_Speed, int Right_Back_Speed) {
    int compensate = 150;  // 用户已放弃调试，保持150
    if(Left_Back_Speed != 0) {
        if(Left_Back_Speed > 0) {
            Left_Front_Speed += compensate;
            Right_Front_Speed -= compensate;
            Right_Back_Speed -= compensate;
        } else {
            Left_Front_Speed -= compensate;
            Right_Front_Speed += compensate;
            Right_Back_Speed += compensate;
        }
    }
    // 限幅保护...
}
```
> **注意**：用户已放弃补偿调试，决定遥控靠手动修正，自主模式让代码处理。

---

## 比赛信息

### 项目类型
物流搬运类比赛：
- 自主识别货物
- 机械臂抓取
- 运送到指定园区（1号区、2号区等）
- **不需要横移**

### 场地特征（根据图片）
- 有黄色放置区（标号1、2）
- 有待派送区（黄色区域放蓝色货物）
- 有黑黄警示带边界
- 左侧有蓝色箭头出发区

---

## 待完成任务（新对话需继续）

### 1. 完善自主模式移动逻辑
当前 `Loop_Vision_Auto()` 只有机械臂动作，**缺少小车移动控制**。

需要补充：
- 从出发区走到待派送区的移动逻辑
- 抓取货物后走到指定园区的移动逻辑
- 可能需要的转向/前进/后退控制

### 2. 需要用户提供的信息
- 自主导航方式：循迹？固定时间/距离跑？视觉定位？
- 货物识别逻辑：ESP32 发什么指令代表看到货物？
- 如何区分1号区和2号区？
- 自主模式的完整流程步骤

---

## 关键文件路径

| 文件 | 路径 |
|------|------|
| 工程文件 | `...\Project\TOPWAY-Qixing32.uvprojx` |
| 主代码 | `...\USER\Main.c` |
| 串口代码 | `...\Hardware\Usart.c` |
| 编译输出 | `...\OUT\TOPWAY-Qixing32.hex` |
| 本日志 | `C:\Users\26630\Desktop\小车调试日志.md` |

---

## 备注
- Keil 编译快捷键：F7（Build），Rebuild 用菜单 Project → Rebuild all target files
- hex 输出目录：`...\02 出厂程序源码\OUT\`
- 烧录工具：FlyMcu
- 波特率：115200
