# CAIR睿抗 · 智械争锋 —— 项目进度日志（2026-07-04 更新）

> 创建时间：2026-07-04
> 用途：Token限额后新窗口快速接续

---

## 一、当前工程状态

### 工程路径
```
C:\Users\26630\Desktop\新建文件夹 (3)\0012-STM32-精灵小车-step1\0012-精灵小车-Step1\02 程序资料\02 出厂程序源码\Project\TOPWAY-Qixing32.uvprojx
```

### 已完成修改（遥控/自主双模式隔离）
官方出厂程序已修改完成，共改动 **4 个文件**：

| 文件 | 修改内容 |
|------|---------|
| `Hardware/Global.h` | 添加 `Sys_Mode`、`rx_cmd` 外部声明 |
| `USER/Main.h` | 添加模式变量声明、`Loop_Vision_Auto()` 函数声明 |
| `USER/Main.c` | 添加变量定义、主循环 `if-else` 分流、SELECT+START 组合切换、新增 Vision 自主处理函数 |
| `Hardware/Usart.c` | `USART2_IRQHandler` 中添加 `0x01`/`0x02` 单字节视觉指令接收 |

### 模式切换方式
| 操作 | 效果 |
|------|------|
| **SELECT + START 同时按下** | 遥控模式 → 视觉自主模式（蜂鸣器响 **3 声**） |
| **SELECT + START 同时按下** | 视觉自主模式 → 遥控模式（蜂鸣器响 **2 声**） |
| **单独按 SELECT** | 保持原有功能：切换 `Auto_Mode`（0→1→2→3→4→0） |

上电默认：**遥控模式**（`Sys_Mode = SYS_MODE_REMOTE`）

---

## 二、下一步操作（按顺序执行）

### 1. 编译工程
- 在 Keil 中点击 **Rebuild**（或按 `F7`）
- 预期结果：`0 Error(s), 0 Warning(s)`
- 如有报错，复制 Build Output 内容给新窗口

### 2. 找到 hex 文件
编译成功后，`.hex` 文件一般在：
```
C:\Users\26630\Desktop\新建文件夹 (3)\0012-STM32-精灵小车-step1\0012-精灵小车-Step1\02 程序资料\02 出厂程序源码\obj\OUTPUT\TOPWAY-Qixing32.hex
```

### 3. FlyMcu 烧录
| 配置项 | 设置值 |
|--------|--------|
| 串口 | CH340 对应的 COM 口 |
| 波特率 | `115200` |
| 左下角选项 | ✅ DTR的低电平复位，RTS高电平进BootLoader |
| 程序文件 | 选择编译出的 `.hex` 文件 |

点击 **开始编程** 烧录。

### 4. 验证
| 操作 | 预期现象 |
|------|---------|
| 上电 | 蜂鸣器 3 声，LED 闪 3 下 |
| 单独按 SELECT | 响 1~4 声（Auto_Mode 切换） |
| SELECT + START | 响 **3 声** → 进入视觉自主模式 |
| SELECT + START | 响 **2 声** → 切回遥控模式 |
| 视觉自主模式下 ESP32 发 `0x01` | 蜂鸣器响 1 声（收到齿轮指令） |
| 视觉自主模式下 ESP32 发 `0x02` | 蜂鸣器响 2 声（收到螺母指令） |

---

## 三、待完成事项

- [ ] Keil 编译通过（0 errors, 0 warnings）
- [ ] FlyMcu 烧录成功
- [ ] 上电验证模式切换（SELECT+START 响 3 声/2 声）
- [ ] ESP32 串口通信验证（发送 0x01/0x02，STM32 响应）
- [x] 填写 `Loop_Vision_Auto()` 中的抓取动作（TODO 位置）—— 已填入非阻塞状态机框架，PWM 值待联调
- [ ] 联调机械臂抓取动作

---

## 四、关键代码速查

### 视觉自主模式处理函数（USER/Main.c）
```c
void Loop_Vision_Auto(void) {
    // 每50ms轮询PS2，检测 SELECT+START 切回遥控
    // 处理 rx_cmd：0x01=齿轮, 0x02=螺母
    // TODO：在 case 0x01 和 case 0x02 中填入实际抓取动作
}
```

### 机械臂总线命令格式
```c
#000P1500T1000!    // 0号舵机转到1500，用时1000ms
#255P1500T2000!    // 255=所有舵机复位
Bususart_Send_Str((u8 *)"#000P1500T1000!#001P2200T1000!...\r\n");
```

### 底盘电机控制
```c
Motors_Run(600, 600, 600, 600);   // 前进
Motors_Run(0, 0, 0, 0);           // 停止
```

---

## 五、硬件接线

### ESP32 ↔ STM32 USART2
| ESP32 | STM32 | 说明 |
|-------|-------|------|
| GPIO17 (TX2) | PA3 (RX) | ESP32发识别结果 |
| GPIO16 (RX2) | PA2 (TX) | 可选回传 |
| GND | GND | **必须共地** |

波特率：115200

### PS2手柄引脚
| 手柄 | STM32 |
|------|-------|
| DAT | PA15 |
| CMD | PA14 |
| ATT | PA13 |
| CLK | PA12 |

---

## 六、踩坑记录

1. **Keil 工程打开路径**
   桌面有两份官方资料，实际工程在 `新建文件夹 (3)` 目录下。
   工程文件：`TOPWAY-Qixing32.uvprojx`
   （不要双击 `.uvguix` 文件）

2. **Keil 不自动刷新外部修改**
   文件被修改后，需要关闭 Keil 重新打开工程，才能看到最新代码。

3. **USART2 已初始化但原工程未使用**
   官方代码里 `Usart2_Init(115200)` 已存在，中断处理也已有，但主循环未使用。
   本次修改在中断里新增了 `rx_cmd` 单字节接收，不破坏原有协议。

4. **关闭 JTAG**
   官方 `Gpio.c` 中 `GPIO_PinRemapConfig(GPIO_Remap_SWJ_JTAGDisable, ENABLE)`
   关闭了 JTAG，保留 SWD。PA15 释放给 PS2 使用。

---

## 七、如需接续（给新窗口看）

**当前最紧急的下一步是：**
> **在 Keil 中点击 Rebuild 编译工程，确认 0 errors 后，用 FlyMcu 烧录到 STM32，然后验证 SELECT+START 模式切换。**

新窗口读取本文件后，可立即接续工作。
